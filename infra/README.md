# icecube.dog infra

This is a terralith monomodule. To keep everything simple, I'm not consuming upstream modules. No variable-based configuration.

## State

state is committed. It contains no secrets.
